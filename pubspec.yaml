name: themeditationapp
description: The Meditation App is a clean, easy to use app, which is designed to help users keep track of how they're feeling during different days and listent to a range of calming, relaxing sounds.


publish_to: 'none'
# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  firebase_auth: ^3.1.4
  cloud_firestore: ^2.5.4
  firebase_core: ^1.10.0

  #Loading spinkit, allows for loading animations
  flutter_spinkit: ^5.1.0

  #HTTP dependency to call Daily Quotes from the internet
  http: ^0.13.4

  #Audioplayer dependecy for creating player for the soundboard
#  audioplayer: ^0.8.1
  audioplayers: ^0.20.1

  #Provides intents to other aspects of Android
  url_launcher: ^6.0.17

  #Google Maps
  google_maps_flutter: ^2.1.1

#  Mood Logs graphs and pie charts
  charts_flutter: ^0.12.0
  pie_chart: ^5.1.0
  sqflite: ^2.0.1
  path_provider: ^2.0.8

  # Provider will help determine if a user is signed in and switch the screens depending on the case
  # Google provided solution
  provider: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^1.0.0


# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true


  assets:
    - assets/images/
    - assets/images/userdashboard/audiowaves.png
    - assets/images/userdashboard/delete.png
    - assets/images/userdashboard/mood.png
    - assets/images/userdashboard/support.png

    - assets/images/moodimages/sad.png
    - assets/images/moodimages/nuteral.png
    - assets/images/moodimages/mediumsad.png
    - assets/images/moodimages/mediumhappy.png
    - assets/images/moodimages/veryhappy.png
